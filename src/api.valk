
use valk:http
use valk:json

error ApiError (err)

fn cdn_get_versions() json:Value !ApiError {
    let resp = http:request("GET", "https://files.valk-cdn.dev/releases/versions.json") ! throw .err
    return json:decode(resp.body) ! throw .err
}
