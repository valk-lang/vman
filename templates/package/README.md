# {{name}}

What this package does, in a sentence.

## Install

```
vman install github.com/user/{{name}}
```

## Example

```rust
use {{name}}

println({{name}}.greeting("valk"))
```

## Development

`make test` runs the tests, `make lint` checks the source, and `make docs` writes the API
reference to `docs/`.
